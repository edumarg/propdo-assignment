import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  table: {
    minWidth: 650,
  },
  positiveChange: {
    color: "green",
  },

  negativeChange: {
    color: "red",
  },

  grey: {
    color: "grey",
  },
  icon: {
    marginTop: 2,
    marginRight: 2,
  },
});

export default useStyles;
